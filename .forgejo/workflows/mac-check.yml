name: mac-check
on: workflow_dispatch
jobs:
  build-mac:
    runs-on: macos
    steps:
      - uses: actions/checkout@v6
      - run: |
          set -eu
          cargo build --locked
          echo "SANTI-MAC-BUILD-OK"
